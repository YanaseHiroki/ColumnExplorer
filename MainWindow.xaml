<Window x:Class="ColumnExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Column Explorer" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左カラム -->
        <ListBox Name="Column1" Grid.Column="0" SelectionChanged="Column1_SelectionChanged"/>

        <!-- 中央カラム -->
        <ListBox Name="Column2" Grid.Column="1" SelectionChanged="Column2_SelectionChanged"/>

        <!-- 右カラム -->
        <ListBox Name="Column3" Grid.Column="2"/>
    </Grid>
</Window>
